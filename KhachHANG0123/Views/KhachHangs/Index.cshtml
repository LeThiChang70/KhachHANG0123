@model IEnumerable<KhachHANG0123.Models.KhachHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label>Mã Khách Hàng</label>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoTenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.KhachHangID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.HoTenKH)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.KhachHangID }) |
        @Html.ActionLink("Details", "Details", new { id = item.KhachHangID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.KhachHangID })
    </td>
</tr>
}

</table>
