@model IEnumerable<KhachHANG0123.Models.NhanVien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoTenNV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoTenNV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NhanVienID }) |
            @Html.ActionLink("Details", "Details", new { id=item.NhanVienID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NhanVienID })
        </td>
    </tr>
}

</table>
