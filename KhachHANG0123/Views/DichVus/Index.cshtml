@model IEnumerable<KhachHANG0123.Models.DichVu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoaiDV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiDV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DichVuID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DichVuID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DichVuID })
        </td>
    </tr>
}

</table>
